To start an Angular 6 project, 

1. Install "npm" if needed
2. Install the Angular CLI by using this command in the command prompt:
    npm install -g @angular/cli
3. Create a new project by using this command in the command prompt:
    ng new [name of project]
4. Go to the folder where the project is stored and use this command in the command prompt:
    ng serve --o
